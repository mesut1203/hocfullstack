// let html = "";

// let number = 15;
// for (let i = 1; i <= number; i++) {
//     // toán tử 3 ngôi
//     const className = i % 2 === 0 ? `class="active"` : "";
//     html = html + `<h2 ${className}>Học lập trình không khó ${i}</h2>`;
// }
// document.body.innerHTML = html;

// cho truocws sooss hangf và số cột
// let html = `<table width="100%" border="1" cellpadding="5">`;
// let rows = 8;
// let cols = 8;

// for (let i = 1; i <= rows; i++) {
//     html = html + `<tr>`;
//     for (j = 1; j <= cols; j++) {
//         const total = i + j;
//         const className = total % 2 !== 0 ? `class="chessSquare"` : "";
//         html = html + `<td ${className}></td>`;
//     }

//     html = html + `</tr>`;
// }

// html = html + `</table>`;
// // console.log(html);

// document.body.innerHTML = html;

